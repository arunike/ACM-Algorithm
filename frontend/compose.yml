services:
  acm-react:
    build: .
    restart: always
    command: npm run dev
    volumes:
      - ./src:/app/src
      - ./public:/app/public
      - ./package.json:/app/package.json
      - ./package-lock.json:/app/package-lock.json
    stdin_open: true
    ports:
      - "5173:5173"